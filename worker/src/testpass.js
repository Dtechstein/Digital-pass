/**
 * testpass.js — thin compatibility layer over the template engine.
 * The Kindness Card is now just the default template (see template.js).
 */
export { buildPassJson, DEFAULT_FIELDS } from './template.js';
